## Monic Polynomial
- An interactive protocol between a prover and a verifier is public-coin if all the messages sent by
the verifier to the prover are randomly and independently sampled
from the messages sent by the prover (that is, random coins from
the verifier are publicly available).